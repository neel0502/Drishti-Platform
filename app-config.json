{
	"command": "sh -c 'if [ -f launcher.py ]; then exec python3 -m uvicorn launcher:app --host 0.0.0.0 --port ${X_ZOHO_CATALYST_LISTEN_PORT:-9000}; else cd .appsail-build && exec python3 -m uvicorn launcher:app --host 0.0.0.0 --port ${X_ZOHO_CATALYST_LISTEN_PORT:-9000}; fi'",
	"build_path": "./.appsail-build",
	"stack": "python_3_11",
	"env_variables": {
		"DRISHTI_DATA_SOURCE": "catalyst",
		"DRISHTI_BOOTSTRAP_DATASTORE": "false"
	},
	"memory": 1024
}
